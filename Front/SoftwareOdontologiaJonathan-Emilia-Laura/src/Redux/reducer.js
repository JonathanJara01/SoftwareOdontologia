const inicialState = {
    contacto: [],
    galeria: {}
};

const reducer = (state = inicialState, action) => {
    switch (action.type) {

    
        default:
            return {...state};
    }
};

export default reducer;